
/**
 * geminiService.ts
 * AI logic removed as per user request.
 */
export const getSearchResponse = async (query: string) => {
  return { text: "AI Disabled", sources: [] };
};

export const getServerInsights = async (stats: any) => {
  return "AI insights disabled.";
};
